info:
  name: registerUser
  type: http
  seq: 3

http:
  method: POST
  url: https://localhost:5400/user
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
